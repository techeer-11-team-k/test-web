# 🧪 Test-Web - API 테스트 레포지토리

## 📋 개요

이 레포지토리는 **Redis를 사용한 가짜 데이터 API 테스트** 환경입니다.

실제 DB를 수정하는 동안 API 동작을 검증할 수 있도록, Redis에 가짜 데이터를 저장하고 테스트합니다.

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   사용자    │ ──▶ │    API      │ ──▶ │   Redis     │
│  (Client)   │     │  (FastAPI)  │     │ (가짜 데이터) │
└─────────────┘     └─────────────┘     └─────────────┘
                          │
                          ▼
                  ┌─────────────┐
                  │  실제 DB    │  ← (수정 중)
                  │ (PostgreSQL)│
                  └─────────────┘
```

## 📁 프로젝트 구조

```
test-web/
├── 📄 readme.md              # 이 파일
├── 🐳 docker-compose.yml     # Docker 설정 (Redis, DB, Backend)
│
├── 📂 api-test/              # 🧪 API 테스트 환경
│   ├── README.md             # 테스트 가이드
│   ├── mock-data/            # 가짜 테스트 데이터
│   │   ├── todos.json        # 할 일 데이터
│   │   ├── users.json        # 사용자 데이터
│   │   └── apartments.json   # 아파트 데이터
│   └── scripts/
│       └── load_mock_data.py # 데이터 로드 스크립트
│
├── 📂 backend/               # ⚙️ FastAPI 백엔드
│   ├── app/
│   │   ├── api/v1/endpoints/
│   │   │   ├── test_api.py   # 🆕 테스트 API 엔드포인트
│   │   │   ├── auth.py
│   │   │   └── admin.py
│   │   └── services/
│   │       └── redis_service.py  # 🆕 Redis 서비스
│   ├── docs/                 # API 문서
│   └── requirements.txt
│
├── 📂 frontend/              # 🖥️ React 프론트엔드
│   └── src/
│       └── components/
│
└── 📂 docs/                  # 📚 문서
    ├── prd.md                # 제품 요구사항 문서
    └── api_docs.md
```

## 🚀 빠른 시작

### 1단계: Docker 서비스 실행

```bash
# Redis + PostgreSQL 실행
docker-compose up -d redis db
```

### 2단계: 가짜 데이터 로드

```bash
cd api-test/scripts
python load_mock_data.py
```

### 3단계: Backend 실행

```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```

### 4단계: API 테스트

```bash
# Swagger UI 열기
open http://localhost:8000/docs

# 또는 curl 테스트
curl http://localhost:8000/api/v1/test/health
curl http://localhost:8000/api/v1/test/todos
```

## 🔗 주요 URL

| 서비스 | URL | 설명 |
|--------|-----|------|
| Swagger UI | http://localhost:8000/docs | API 문서 및 테스트 |
| ReDoc | http://localhost:8000/redoc | API 문서 (읽기용) |
| Backend API | http://localhost:8000/api/v1 | API 루트 |
| Frontend | http://localhost:5173 | (로컬 실행 시) |

## 📝 테스트 API 목록

### 🧪 Test API (`/api/v1/test/`)

| 메서드 | 경로 | 설명 |
|--------|------|------|
| `GET` | `/health` | Redis 연결 상태 확인 |
| `GET` | `/todos` | 할 일 목록 |
| `POST` | `/todos` | 할 일 생성 |
| `GET` | `/apartments` | 아파트 검색 |
| `GET` | `/users` | 사용자 목록 |

자세한 내용: [api-test/README.md](./api-test/README.md)

## 📚 문서

- [📋 PRD (제품 요구사항)](./docs/prd.md)
- [🧪 API 테스트 가이드](./api-test/README.md)
- [⚙️ API 개발 가이드](./backend/docs/api_development.md)
- [🔐 인증 플로우](./backend/docs/auth_flow.md)

## ⚠️ 참고사항

- 이 레포지토리는 **테스트 용도**입니다.
- 실제 서비스는 `techeer-team-b-2026` 레포지토리를 참고하세요.
- DB 수정이 완료되면 실제 DB로 전환하세요.
