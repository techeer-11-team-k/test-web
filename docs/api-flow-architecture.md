# API 흐름 아키텍처 설명

## 📋 전체 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                        클라이언트 (Client)                       │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Expo (React Native + TypeScript + TailwindCSS)          │   │
│  │  - 모바일 앱 (iOS/Android)                               │   │
│  │  - KakaoMap 통합                                         │   │
│  └──────────────────────────────────────────────────────────┘   │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                               │ HTTPS 요청
                               │ GET /api/v1/search/apartments?q=래미안
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│                    AWS EC2 (Amazon Cloud)                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Docker Container                                         │   │
│  │                                                            │   │
│  │  ┌────────────────────────────────────────────────────┐   │   │
│  │  │  NGINX (Reverse Proxy & Load Balancer)            │   │   │
│  │  │  - 포트: 80, 443                                    │   │   │
│  │  │  - SSL/TLS 인증서 처리                             │   │   │
│  │  │  - 요청 라우팅 및 로드 밸런싱                      │   │   │
│  │  └────────────────────────────────────────────────────┘   │   │
│  │                    │                                       │   │
│  │                    │ 내부 요청 전달                        │   │
│  │                    ↓                                       │   │
│  │  ┌────────────────────────────────────────────────────┐   │   │
│  │  │  FastAPI (Python Backend)                            │   │   │
│  │  │  - 포트: 8000 (내부)                                 │   │   │
│  │  │  - API 엔드포인트 처리                               │   │   │
│  │  │  - 비즈니스 로직 실행                                │   │   │
│  │  └────────────────────────────────────────────────────┘   │   │
│  │                    │                                       │   │
│  │        ┌───────────┼───────────┐                         │   │
│  │        │           │           │                         │   │
│  │        ↓           ↓           ↓                         │   │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐                │   │
│  │  │ Redis   │  │PostgreSQL│  │Amazon S3 │                │   │
│  │  │ (Cache) │  │ (RDS)    │  │ (Storage)│                │   │
│  │  └─────────┘  └─────────┘  └─────────┘                │   │
│  │                                                            │   │
│  │  ┌────────────────────────────────────────────────────┐   │   │
│  │  │  Monitoring (Prometheus + Grafana)                 │   │   │
│  │  │  - 메트릭 수집 및 시각화                           │   │   │
│  │  │  - Slack 알림                                      │   │   │
│  │  └────────────────────────────────────────────────────┘   │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                               │
                               │ 외부 API 호출
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│                    외부 서비스 (External APIs)                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                    │
│  │  KOSIS   │  │  Google  │  │  Kakao   │                    │
│  │ (통계청) │  │  Maps    │  │  (지도)  │                    │
│  └──────────┘  └──────────┘  └──────────┘                    │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 API 요청 흐름 상세

### 1. 클라이언트 → NGINX

```
클라이언트 (Expo 앱)
    │
    │ HTTPS 요청
    │ GET https://api.example.com/api/v1/search/apartments?q=래미안&limit=10
    │ Headers:
    │   - Authorization: Bearer <token>
    │   - Content-Type: application/json
    │
    ↓
NGINX (포트 443)
    │
    │ - SSL/TLS 인증서 검증
    │ - 요청 로깅
    │ - Rate Limiting (DDoS 방어)
    │ - 정적 파일 서빙 (있는 경우)
    │
    ↓
내부 라우팅 규칙 적용
    │
    │ location /api/ {
    │     proxy_pass http://fastapi:8000;
    │     proxy_set_header Host $host;
    │     proxy_set_header X-Real-IP $remote_addr;
    │ }
    │
    ↓
FastAPI로 요청 전달
```

### 2. NGINX → FastAPI

```
NGINX
    │
    │ HTTP 요청 (내부 네트워크)
    │ GET http://fastapi:8000/api/v1/search/apartments?q=래미안&limit=10
    │ Headers:
    │   - Host: api.example.com
    │   - X-Real-IP: <client_ip>
    │
    ↓
FastAPI (포트 8000)
    │
    │ 1. 요청 수신 (app/main.py)
    │ 2. 라우터 매칭 (app/api/v1/router.py)
    │ 3. 엔드포인트 라우팅 (app/api/v1/endpoints/search.py)
    │
    ↓
search_apartments() 함수 실행
```

### 3. FastAPI → 데이터 소스 (현재: Redis / 향후: PostgreSQL)

#### 현재 구조 (DB 수정 중 - Redis 사용)

```
FastAPI (search.py)
    │
    │ # Redis 캐시에서 데이터 조회
    │ redis_svc = get_redis_service()
    │ apartments_data = redis_svc.search_apartments_by_name(q, limit)
    │
    ↓
Redis (포트 6379)
    │
    │ GET apartments
    │ → JSON 문자열 반환
    │ → Python 리스트로 파싱
    │ → 필터링 및 정렬
    │
    ↓
검색 결과 반환
    │
    │ [
    │   {
    │     "apt_id": 1,
    │     "apt_name": "래미안 강남파크",
    │     ...
    │   },
    │   ...
    │ ]
    │
    ↓
FastAPI 응답 구성
```

#### 향후 구조 (DB 수정 완료 - PostgreSQL 사용)

```
FastAPI (search.py)
    │
    │ # SQLAlchemy ORM으로 DB 쿼리
    │ result = await db.execute(
    │     select(Apartment)
    │     .where(Apartment.apt_name.ilike(f"%{q}%"))
    │     .order_by(Apartment.apt_name)
    │     .limit(limit)
    │ )
    │
    ↓
PostgreSQL (Amazon RDS)
    │
    │ SQL 쿼리 실행:
    │ SELECT * FROM apartments
    │ WHERE apt_name ILIKE '%래미안%'
    │ ORDER BY apt_name
    │ LIMIT 10;
    │
    │ → 인덱스 활용 (apt_name 인덱스)
    │ → 빠른 검색 수행
    │
    ↓
검색 결과 반환
    │
    │ Apartment 객체 리스트
    │
    ↓
FastAPI 응답 구성
```

### 4. FastAPI → 외부 API (선택적)

```
FastAPI
    │
    │ # 외부 API 호출 (필요한 경우)
    │ - KOSIS: 통계 데이터 조회
    │ - Google Maps: 지오코딩, 역지오코딩
    │ - Kakao: 지도 데이터, 인증
    │
    ↓
외부 API 서버
    │
    │ HTTP 요청
    │ GET https://api.kosis.kr/...
    │ GET https://maps.googleapis.com/...
    │
    ↓
외부 API 응답
    │
    │ JSON 데이터
    │
    ↓
FastAPI에서 데이터 처리 및 통합
```

### 5. FastAPI → Amazon S3 (선택적)

```
FastAPI
    │
    │ # 파일 업로드/다운로드
    │ - 사용자 프로필 이미지
    │ - 아파트 사진
    │ - 문서 파일
    │
    ↓
Amazon S3
    │
    │ PUT / GET 요청
    │ → 파일 저장 / 조회
    │
    ↓
S3 URL 반환
    │
    │ https://s3.amazonaws.com/bucket/file.jpg
    │
    ↓
FastAPI 응답에 포함
```

### 6. FastAPI → 클라이언트 (응답)

```
FastAPI
    │
    │ 응답 데이터 구성
    │ {
    │   "success": true,
    │   "data": {
    │     "results": [...]
    │   },
    │   "meta": {
    │     "query": "래미안",
    │     "count": 10
    │   }
    │ }
    │
    ↓
NGINX
    │
    │ - 응답 버퍼링
    │ - 압축 (gzip)
    │ - 로깅
    │
    ↓
클라이언트 (Expo 앱)
    │
    │ JSON 응답 수신
    │ → 파싱 및 UI 업데이트
    │ → 검색 결과 표시
```

## 📊 실제 API 호출 예시

### 요청 (클라이언트 → 서버)

```http
GET /api/v1/search/apartments?q=래미안&limit=10 HTTP/1.1
Host: api.example.com
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
Content-Type: application/json
User-Agent: Expo/ReactNative
```

### 처리 과정

1. **NGINX** (포트 443)
   - SSL/TLS 인증서 검증
   - 요청을 `http://fastapi:8000`로 프록시

2. **FastAPI** (포트 8000)
   - 라우터 매칭: `/api/v1/search/apartments`
   - 엔드포인트: `search_apartments()`
   - 파라미터 파싱: `q="래미안"`, `limit=10`

3. **데이터 조회** (현재: Redis)
   ```python
   # Redis에서 데이터 조회
   redis_svc = get_redis_service()
   apartments = redis_svc.search_apartments_by_name("래미안", 10)
   ```

4. **응답 구성**
   ```python
   return {
       "success": True,
       "data": {"results": apartments},
       "meta": {"query": "래미안", "count": len(apartments)}
   }
   ```

### 응답 (서버 → 클라이언트)

```http
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 1234
X-Response-Time: 45ms

{
  "success": true,
  "data": {
    "results": [
      {
        "apt_id": 1,
        "apt_name": "래미안 강남파크",
        "address": "서울특별시 강남구 역삼동 123-45",
        "sigungu_name": "강남구",
        "dong_name": "역삼동",
        "location": {
          "lat": 37.5012,
          "lng": 127.0375
        }
      },
      ...
    ]
  },
  "meta": {
    "query": "래미안",
    "count": 10
  }
}
```

## 🔄 전체 요청-응답 사이클

```
[1] 클라이언트 요청 생성
    Expo 앱 → 사용자가 "래미안" 검색 입력
    │
[2] HTTP 요청 전송
    GET /api/v1/search/apartments?q=래미안
    │
[3] NGINX 수신
    SSL 검증 → 로깅 → 라우팅
    │
[4] FastAPI 처리
    라우터 매칭 → 엔드포인트 실행
    │
[5] 데이터 조회
    Redis/PostgreSQL에서 검색
    │
[6] 응답 구성
    JSON 형식으로 데이터 변환
    │
[7] NGINX 응답 전송
    압축 → 로깅 → SSL 암호화
    │
[8] 클라이언트 수신
    JSON 파싱 → UI 업데이트
```

## 🎯 주요 포인트

### 1. NGINX의 역할
- **Reverse Proxy**: 외부 요청을 내부 FastAPI로 전달
- **SSL/TLS**: HTTPS 인증서 처리
- **로드 밸런싱**: 여러 FastAPI 인스턴스 간 부하 분산
- **정적 파일 서빙**: 이미지, CSS, JS 등

### 2. FastAPI의 역할
- **API 엔드포인트**: RESTful API 제공
- **비즈니스 로직**: 검색, 필터링, 정렬 등
- **데이터 변환**: DB 데이터 → JSON 응답

### 3. 데이터 소스 선택
- **현재**: Redis (테스트용 더미 데이터)
- **향후**: PostgreSQL (실제 운영 데이터)
- **캐싱**: Redis를 2차 캐시로 활용 가능

### 4. 모니터링
- **Prometheus**: 메트릭 수집 (응답 시간, 에러율 등)
- **Grafana**: 대시보드 시각화
- **Slack**: 알림 전송

## 📚 관련 파일

- `test-web/backend/app/api/v1/endpoints/search.py`: 검색 API 엔드포인트
- `test-web/backend/app/api/v1/router.py`: API 라우터
- `test-web/backend/app/main.py`: FastAPI 앱 진입점
- `test-web/backend/app/services/redis_service.py`: Redis 서비스

## 🔍 디버깅 팁

### 요청 추적
```bash
# NGINX 로그 확인
docker logs nginx-container

# FastAPI 로그 확인
docker logs fastapi-container

# Redis 로그 확인
docker logs redis-container
```

### API 테스트
```bash
# 직접 FastAPI 호출 (NGINX 우회)
curl http://localhost:8000/api/v1/search/apartments?q=래미안

# NGINX를 통한 호출
curl https://api.example.com/api/v1/search/apartments?q=래미안
```
